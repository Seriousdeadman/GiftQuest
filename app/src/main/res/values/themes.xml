<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.GiftQuest" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Splash theme -->
    <style name="Theme.GiftQuest.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.GiftQuest</item>
    </style>
    <style name="zxing_CaptureTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>